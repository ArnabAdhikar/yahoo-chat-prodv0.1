# MongoDB Persistent Volume

apiVersion: v1
kind: PersistentVolume
metadata:
  name: mongodb-pv
  namespace: yahoo-chat
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 5Gi
  hostPath:
    path: /data